비동기 패턴
1. 콜백 함수 : 다른 함수의 매개변수로 사용
2. 프라미스 : 노드 모듈에는 프라미스를 변환하는 함수가 따로 존재
then(비동기 처리 성공), catch(비동기 처리 실패)
3. async/await : 에크마스크립트부터 새로 등장
비동기 처리 함수를 감싸는 함수를 async로 붙이고, 처리할 함수는 await로 설정
- await를 붙일 수 있는 함수는 promise를 반환하는 메서드